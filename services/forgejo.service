[Unit]
Description=Forgejo Git Server
Requires=postgres.service
After=network.target postgres.service

[Service]
Type=simple
ExecStart=/etc/homelab/homelab-ctl run forgejo
ExecStop=/etc/homelab/homelab-ctl stop forgejo
Restart=always

[Install]
WantedBy=multi-user.target
