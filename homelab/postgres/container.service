[Unit]
Description=PostgreSQL container
After=network.target

[Service]
Type=simple
ExecStart=/etc/homelab/postgres/podman-run.sh
ExecStop=/usr/bin/podman stop postgres
ExecStopPost=/usr/bin/podman rm -f postgres
Restart=always

[Install]
WantedBy=multi-user.target
